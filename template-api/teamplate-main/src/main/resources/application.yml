server:
  port: 28080
  servlet:
    context-path: /template-api
spring:
  application:
    name: template-api
  profiles:
    # local, dev, prod
    default: local
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui